---
# Main playbook — provision and deploy
- name: Provision web servers
  hosts: webservers
  become: yes

  roles:
    - common
    - docker

- name: Deploy application
  hosts: webservers
  become: yes

  roles:
    - app_deploy
