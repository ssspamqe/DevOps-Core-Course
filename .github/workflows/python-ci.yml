name: Python Application CI/CD

on:
  push:
    branches: [ "main", "lab03" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f app_python/requirements.txt ]; then pip install -r app_python/requirements.txt; fi

    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 app_python --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 app_python --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Test with pytest
      run: |
        # Run tests from the app_python directory to handle relative imports if any
        # or properly point to the test directory and python path
        cd app_python
        pytest --cov=. tests/

  docker-build-push:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Extract version
      id: meta
      run: |
        # Extract version from app.py
        VERSION=$(grep -o '"version": "[^"]*"' app_python/app.py | cut -d'"' -f4)
        echo "VERSION=$VERSION" >> $GITHUB_ENV
        echo "Detected version: $VERSION"

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./app_python
        push: true
        tags: |
          ${{ secrets.DOCKER_USERNAME }}/devops-info-service:latest
          ${{ secrets.DOCKER_USERNAME }}/devops-info-service:${{ env.VERSION }}
        cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/devops-info-service:buildcache
        cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/devops-info-service:buildcache,mode=max
